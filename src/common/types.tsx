export type tipoMusica = {
    _id: string,
    nome: string,
    artista: string,
    genero: string,
    vibes: string,
    link: string
}

export type tipoPesquisa = {
    filtro: string,
    pesquisa: string
}